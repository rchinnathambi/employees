server:
  port: 7051
  
spring:
  application:
    name: employees
  datasource:
    url: jdbc:h2:file:~/workspace/db/employee;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
